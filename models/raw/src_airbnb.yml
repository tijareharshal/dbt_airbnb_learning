version: 2

sources:
  - name: airbnb
    database: raw
    schema: airbnb
    tables:
      - name: listings
        identifier: raw_listings

      - name: hosts
        identifier: raw_hosts

      - name: reviews
        identifier: raw_reviews
        