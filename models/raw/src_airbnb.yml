vsersion: 2

sources:
  - name: airbnb
    database: raw
    schema: airbnb
    tables:
      -name: hosts
        identifier: raw_hosts

      -name: reviews
        identifier: raw_reviews

      -name: listings
        identifier: raw_listings